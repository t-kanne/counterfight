<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="de.woodpot.counterfight.MainActivity" >

    <Button
        android:id="@+id/btnShowAllCountersOfGroup"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:text="Alle Counter dieser Gruppe anzeigen" />

    <TextView
        android:id="@+id/txtMainCounter"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/btnCountUp"
        android:layout_alignLeft="@+id/btnCountUp"
        android:layout_marginBottom="43dp"
        android:text="TextView"
        android:textSize="20sp" />

    <Button
        android:id="@+id/btnCountUp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/btnRefreshCounter"
        android:layout_centerHorizontal="true"
        android:text="+1" />

    <Button
        android:id="@+id/btnRefreshCounter"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/btnShowAllCountersOfGroup"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="37dp"
        android:text="Aktualisieren" />

</RelativeLayout>